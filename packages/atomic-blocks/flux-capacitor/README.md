# ⚡ FLUX_CAPACITOR — Temporal-Energy Execution Core

**Authority:** MX2⟁☣
**Domain:** XCFE ⟂ K'UHUL π ⟂ CSS Runtime
**Class:** Temporal–Energy Execution Core
**Status:** Canonical / Non-Optional

---

## Prime Law (Non-Negotiable)

```text
No component may create, measure, or advance time outside FLUX_CAPACITOR.
```

All clocks, phases, deltas, cadences, and energy transitions **must route through these opcodes**.

---

## Architectural Position

```
┌─────────────────────────────────────────────────────┐
│                ⚛️ ATOMIC GLYPH LANGUAGE             │
│    Glyphs = Executable Semantic Atomic Blocks        │
└───────────────────┬─────────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────────┐
│               MX2⟁☣ HAZARD GOVERNANCE              │
│       Enforcement of Execution Boundaries           │
└───────────────────┬─────────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────────┐
│           FLUX_CAPACITOR Temporal Spine            │
│   Time/Energy/Phase Synchronization Authority       │
└───────┬──────────────────────┬──────────────────────┘
        │                      │
┌───────▼──────┐     ┌─────────▼──────────────┐
│   CSS RUNTIME    │     │     C@@L KERNEL        │
│  Visual Execution│     │  Block Processing &    │
│    Surface       │     │  Self-Learning (@gram) │
└──────────────────┘     └────────────────────────┘
```

---

## Opcode Taxonomy

The FLUX_CAPACITOR opcode set is divided into **five domains**:

| Domain | Purpose |
|--------|---------|
| **Tick & Phase** | Time advancement and execution phases |
| **Delta & Timebase** | Timing calculations and synchronization |
| **Energy & Entropy** | System energy and disorder management |
| **Synchronization** | Cross-system coordination |
| **Control & Safety** | MX2⟁☣ enforced governance |

Each opcode is:
- Deterministic
- Serializable
- Replayable
- XCFE-addressable
- π-executable
- CSS-synchronizable

---

## Core Opcodes

### Tick & Phase

| Opcode | Purpose |
|--------|---------|
| `@flux.init` | Initialize the temporal spine |
| `@flux.tick` | Advance exactly one lawful tick |
| `@flux.phase` | Declare or transition execution phase |

### Delta & Timebase

| Opcode | Purpose |
|--------|---------|
| `@flux.delta` | Retrieve current tick delta |
| `@flux.time` | Read absolute lawful time |
| `@flux.align` | Align sub-systems to same timebase |

### Energy & Entropy

| Opcode | Purpose |
|--------|---------|
| `@flux.energy` | Set or query system energy level |
| `@flux.entropy` | Govern entropy accumulation/decay |
| `@flux.stabilize` | Actively reduce entropy |

### Synchronization

| Opcode | Purpose |
|--------|---------|
| `@flux.sync` | Synchronize CSS runtime with π + XCFE |
| `@flux.barrier` | Prevent phase advancement until ready |

### Control & Safety

| Opcode | Purpose |
|--------|---------|
| `@flux.halt` | Immediately stop time advancement |
| `@flux.resume` | Resume lawful ticking after halt |
| `@flux.snapshot` | Capture full temporal state |

---

## Forbidden Operations

The following **must never exist**:

- `setInterval`
- `setTimeout` as authority
- `requestAnimationFrame` as clock
- `Date.now()` for logic
- `performance.now()` for causality

JS may *observe* time only through:
- `@flux.time`
- `@flux.delta`
- `@flux.snapshot`

---

## XCFE Integration

| Flux Opcode | XCFE Role |
|-------------|-----------|
| `@flux.tick` | flow advance |
| `@flux.phase` | execution gate |
| `@flux.energy` | state variable |
| `@flux.sync` | flow barrier |
| `@flux.halt` | emergency stop |

Time is now a **first-class flow**, not a side effect.

---

## Files

```
flux-capacitor/
├── README.md           # This file
├── core.pi             # K'UHUL π opcode implementations
├── opcodes.json        # Opcode → AST mappings
├── flux.css            # CSS runtime integration
├── flux-hud.html       # Visualization HUD
└── flux-sw.js          # Service Worker bridge
```

---

## Canonical Locks

### Lock 1: Temporal Sovereignty
```
FLUX_CAPACITOR owns time. All clocks, intervals, and animations
must derive from its opcodes. JS may observe, never control.
```

### Lock 2: Phase Governance
```
Execution phases are gates. Mixed-phase execution is a structural
violation. Phase transitions are governed, not free.
```

### Lock 3: Energy Conservation
```
System energy is conserved. Entropy accumulates and must be
actively managed. Unbounded energy is impossible.
```

### Lock 4: Hazard Integration
```
Temporal hazards are MX2⟁☣ violations. Halt is immediate,
resume requires verification. No silent recovery.
```

### Lock 5: CSS Synchronization
```
CSS runtime variables are controlled by FLUX_CAPACITOR.
Visual timing is deterministic, not emergent.
```

---

## Canonical One-Line Lock

```text
The FLUX_CAPACITOR opcode set is the sole lawful interface for time, phase,
energy, and synchronization in an Atomic system, enforcing deterministic
causality under MX2⟁☣ authority.
```

---

```
STATUS: CANONICAL
VERSION: 1.0.0
GLYPH: ⚡
AUTHORITY: MX2⟁☣
```
